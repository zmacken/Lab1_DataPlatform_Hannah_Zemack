[project]
name = "lab1-dataplatfrom-hannah-zemack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.0",
    "psycopg[binary,pool]>=3.3.2",
]
